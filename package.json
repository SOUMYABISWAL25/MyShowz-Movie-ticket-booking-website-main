{
  "name": "myshowz-movie-booking",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npx ampx sandbox",
    "deploy": "npx ampx pipeline-deploy --branch main --app-id $APP_ID"
  },
  "dependencies": {
    "serverless-http": "^3.2.0",
    "aws-amplify": "^6.0.0"
  },
  "devDependencies": {
    "@aws-amplify/backend": "^1.0.0",
    "@aws-amplify/backend-cli": "^1.0.0",
    "typescript": "^5.0.0"
  }
}
